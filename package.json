{
    "name": "serpent-showdown-root",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "dev": "concurrently \"npm run dev --prefix frontend\" \"uv run --project backend uvicorn backend.main:app --reload\"",
        "install:all": "npm install && npm install --prefix frontend && uv sync --project backend"
    },
    "devDependencies": {
        "concurrently": "^9.1.0"
    }
}