services:
  - type: web
    name: serpent-showdown
    env: docker
    plan: free
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: serpent-db
          property: connectionString
      - key: STATIC_DIR
        value: /app/static

databases:
  - name: serpent-db
    databaseName: serpent_showdown
    plan: starter
